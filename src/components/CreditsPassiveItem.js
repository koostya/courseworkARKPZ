import React, { Component } from 'react'
import { connect } from 'react-redux'

 
class CreditsPassiveItem extends Component {
    render() {
        return(
            <div className="credits_passive_item">
                
            </div>
        );
    }
}

export default connect(state => ({
})
)(CreditsPassiveItem)
